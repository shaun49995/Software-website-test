name: Deploy to GitHub Pages

on:
  push:
    branches:
      - claude/3d-integration-website-lvmua0
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Allow one concurrent deployment; newer pushes cancel in-progress builds.
concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Configure Pages
        id: pages
        uses: actions/configure-pages@v5
        with:
          enablement: true

      - run: npm ci

      - name: Build static site
        env:
          STATIC_EXPORT: "1"
          # Served from https://<owner>.github.io/<repo>/ , so assets live under /<repo>/.
          BASE_PATH: "/${{ github.event.repository.name }}/"
        run: npm run build

      - name: Prepare artifact (SPA fallback + bypass Jekyll)
        run: |
          cp build/client/index.html build/client/404.html
          touch build/client/.nojekyll

      - uses: actions/upload-pages-artifact@v3
        with:
          path: build/client

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
